{
  "name": "quickfit-photo-analysis",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrations:apply:local": "wrangler d1 migrations apply quickfit-db --local",
    "migrations:apply:remote": "wrangler d1 migrations apply quickfit-db --remote"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
